import type { Flow, PromptInput, StageId, TrackId } from '@shared/types'
import { STAGE_COLORS } from '@shared/types'
import type { PromptRepository } from './store/repository'

/**
 * Built-in route: the steps of a project in order, one prompt per step, with a
 * variant per project type wherever the type changes what to do. This is the product —
 * the route is empty without it. Seeded once, when no step (category with a
 * stage) exists yet, so an upgrade from the pre-stage data format gets the
 * built-ins alongside whatever it already had. Users edit these like any other
 * prompt.
 *
 * Prompt rules: at most one {{variable}} and it is one sentence; every step
 * reads the file the previous step wrote and writes the next, so the user
 * never carries text between steps.
 */
export function seedIfEmpty(repo: PromptRepository): void {
  if (repo.listCategories().some((c) => c.stage)) return

  for (const step of STEPS) {
    const cat = repo.createCategory({
      name: step.name,
      color: STAGE_COLORS[step.stage],
      stage: step.stage,
      hint: step.hint,
      output: step.output,
      flow: step.flow
    })
    const variants: [TrackId | null, string][] = step.byTrack
      ? (Object.entries(step.byTrack) as [TrackId, string][])
      : [[null, step.prompt!]]
    for (const [track, content] of variants) {
      const input: PromptInput = {
        title: step.name,
        description: step.hint,
        categoryId: cat.id,
        track,
        content,
        variables: step.question
          ? [{ name: '一句话', label: step.question, type: 'multiline', required: true }]
          : []
      }
      repo.createPrompt(input)
    }
  }
}

interface SeedStep {
  stage: StageId
  name: string
  hint: string
  output: string
  flow?: Flow
  /** label of the single {{一句话}} input, when the step takes one */
  question?: string
  prompt?: string
  byTrack?: Record<TrackId, string>
}

/** Shared head of 说清想法; each type adds the questions that only matter for it. */
const idea = (mustAsk: string): string => `我想做：{{一句话}}

先别给方案，也别写代码。你现在是我的产品搭档，帮我把想法问清楚：
- 一次只问一个问题，等我回答再问下一个。我答得含糊就追问，给我 2–3 个具体选项帮我选。
- 通用的必问：给谁用、他现在是怎么解决的、用了这个之后哪里不一样、第一版做成什么样算成功、明确不做什么。
- 这类项目还要问清：${mustAsk}
- 已经清楚的不要再问。8 个问题内结束。
问完把结论写到 docs/01-想法.md（没有 docs 文件夹就先建一个）：一句话定位、目标用户和使用场景、要解决的问题、成功标志、明确不做的事、还没定的问题。
写完用三句话给我复述一遍，我确认了再进下一步。`

/** Shared head of 划 MVP 范围; each type lists what first versions of that kind usually cut. */
const scope = (usuallyCut: string): string => `读 docs/01-想法.md。

帮我把第一版砍到最小：
- 列出所有想到的功能，每条标：必须有 / 有更好 / 以后再说。
- 「必须有」只留 3–5 条。标准：少了它，用户就完成不了那一件核心的事。能用手工、表格、群聊代替的一律砍。
- 这类项目第一版通常砍掉的：${usuallyCut}。逐条问我要不要。
- 每条「必须有」写一句"用户能做到什么"，不写怎么实现。
- 列出第一版故意接受的粗糙之处（比如没有找回密码、只支持一个人用），免得后面被当成 bug。
写到 docs/02-范围.md。最后一句话告诉我：第一版做完，用户能干什么。`

/** Shared body of 初始化项目; only the "what you should see at the end" line differs per type. */
const init = (finalCheck: string): string => `读 docs/04-技术栈.md 和 docs/05-架构.md。

按技术栈初始化项目。直接动手，每一步做完都实际跑一下确认能用：
1. 用官方脚手架创建工程（用文档里定的版本），装依赖。锁文件提交进仓库。
2. 配好：严格模式的类型检查、Lint + 格式化、测试框架加一个示例测试、.gitignore（含 .env、构建产物、系统文件）、.env.example（列出每个环境变量和它是干什么的）。
3. 脚本齐全：dev / build / test / lint / typecheck，每个都跑一遍通过。
4. 按 docs/05-架构.md 建目录，空文件夹放一个 README 说用途。
5. 写 CLAUDE.md，是给你自己以后每次开工看的项目规则：怎么跑、怎么测、代码约定（从架构文档抄过来）、改动前先读相关文件、改完必须跑 typecheck + lint + test、密钥不进代码、不确定就问我。
6. README：一句话介绍 + 本机怎么跑起来。
7. git init，做第一次提交。
${finalCheck}`

/** Shared head of 审一遍; each type appends its own security / correctness checklist. */
const review = (checklist: string): string => `审查这个功能从开始到现在的所有改动（用 git log 找到功能开始前的提交，git diff 看差异）。

按严重程度列问题，每条写：位置 / 问题 / 怎么改：
1. 会出错或丢数据：异常没处理、竞态、事务、边界。
2. 安全，这类项目重点查：${checklist}
3. 违反 docs/05-架构.md 或 CLAUDE.md 里的约定。
4. 可读性：以后改会踩坑的地方。
只报真问题，不报风格偏好。1 和 2 直接修，修完跑全部检查和测试，再提交；3 和 4 列出来问我要不要改。
最后回答：这个功能的验收标准是不是每条都达到了？没达到的列出来。`

const STEPS: SeedStep[] = [
  // ---- 想清楚 ----
  {
    stage: 'think',
    flow: 'fresh',
    name: '说清想法',
    question: '你想做什么？',
    hint: '一句话就行。AI 会一个一个问你，把想法问清楚，不用你懂技术。',
    output: 'docs/01-想法.md',
    byTrack: {
      web: idea('要不要登录（还是先不要账号）、是公开给所有人还是只给自己或团队用、数据要不要多人共用、主要在电脑上用还是手机浏览器'),
      cli: idea('谁在什么机器上跑（自己一台、团队、公开发布）、输入从哪来（参数、文件、管道、网络）、输出是给人看还是给别的程序读、会不会在 CI 或脚本里无人值守地跑'),
      desk: idea('Windows 和 macOS 都要还是先做一个、数据只放本机还是要多台设备同步、要不要常驻后台或托盘、离线能不能用、要不要开机自启'),
      mobile: idea('iOS 和 Android 都要还是先做一个、要不要账号、没网的时候要能用吗、要不要推送通知、是上架商店还是只内部分发'),
      other: idea('在哪运行、谁或什么事件触发它、输入输出分别是什么、跑失败了谁会知道')
    }
  },
  {
    stage: 'think',
    flow: 'fresh',
    name: '划 MVP 范围',
    hint: '想法有了，现在决定第一版只做什么。砍得越狠，越快做出来。',
    output: 'docs/02-范围.md',
    byTrack: {
      web: scope('找回密码、第三方登录、后台管理页（先用数据库工具直接改）、邮件通知、付费、多语言、SEO、手机端适配'),
      cli: scope('交互式向导、插件机制、自动更新、进度条和花哨输出、多种输出格式、用不到的操作系统'),
      desk: scope('自动更新、代码签名（内测阶段先绕过）、多窗口、托盘、多语言、云同步、插件、快捷键自定义'),
      mobile: scope('先只做一个平台、推送、社交分享、深色模式、多语言、应用内购买、离线、平板适配'),
      other: scope('配置项、多种输入输出格式、并发、重试、监控告警')
    }
  },
  {
    stage: 'think',
    flow: 'fresh',
    name: '写需求文档',
    hint: '把前两步变成一份文档，后面每一步 AI 都会读它。你只需要拍板。',
    output: 'docs/03-需求.md',
    prompt: `读 docs/01-想法.md 和 docs/02-范围.md。

写一份后面每一步都会依赖的需求文档到 docs/03-需求.md，1000 字以内：
- 背景与目标，两三句。
- 每个「必须有」功能一段：用户故事（作为…我想…以便…）+ 验收标准。验收标准写成可以逐条打勾的具体行为，包括出错的时候应该怎样（输入为空、重复提交、没网、没权限）。
- 数据：用户会输入什么，系统要记住什么，哪些数据丢了不能接受。
- 明确不做的事。
- 非功能要求：只写真的需要的（手机上能用、断网能用、数据不能丢、多少人同时用、响应多快算慢）。
写完列出你不确定、需要我拍板的点，一次一个问我。`
  },

  // ---- 定方案 ----
  {
    stage: 'plan',
    flow: 'fresh',
    name: '选技术栈',
    question: '你会什么？完全不会也可以写「不会」',
    hint: '不用你懂技术。AI 按项目和你的情况选最省事、最不容易出问题的一套。',
    output: 'docs/04-技术栈.md',
    byTrack: {
      web: `读 docs/03-需求.md。我的情况：{{一句话}}

为这个网站选一套技术栈，写到 docs/04-技术栈.md。选择原则：
- 成熟、文档全、你最熟的。不选新出的、小众的——出了问题我查不到答案。
- 越少越好：一个语言、一个框架、一个数据库、一个部署平台。前后端一体的全栈框架优先，第一版不拆前后端两个仓库。
- 按我的水平选，我不会就选最不需要配置的。
- 第一版免费或接近免费就能跑起来；数据库选托管的，不自己装。
要定的：全栈框架、数据库和 ORM / 迁移工具、认证方式（先考虑框架或托管服务自带的）、部署平台、包管理器、UI 组件库（有就不自己写样式）。每项写：选什么、版本、一句话为什么、一个备选。
最后写清本机要先装什么（Node 版本、数据库客户端等）和验证装好了的命令，再用大白话说一遍这套东西是怎么配合的。`,
      cli: `读 docs/03-需求.md。我的情况：{{一句话}}

为这个命令行工具选一套技术栈，写到 docs/04-技术栈.md。选择原则：
- 成熟、文档全、你最熟的。不选小众的。
- 越少越好，依赖能不加就不加；启动要快，用户每敲一次都在等。
- 按我的水平选。
- 用户装起来要简单：一条命令，最好不用先装运行时。
要定的：语言与运行时、参数解析库、配置文件格式和位置、测试框架、分发方式（npm / 单文件二进制 / Homebrew / pip）。每项写：选什么、版本、一句话为什么、一个备选。
最后写清本机要先装什么和验证命令，再用大白话说一遍。`,
      desk: `读 docs/03-需求.md。我的情况：{{一句话}}

为这个桌面应用选一套技术栈，写到 docs/04-技术栈.md。选择原则：
- 成熟、文档全、你最熟的。不选小众的。
- 按我的水平选：Electron 上手容易、安装包大、内存高；Tauri 小而快但要会 Rust、生态少。说清楚差别再推荐。
- 打包、签名、自动更新是桌面应用最容易卡住的三件事，现在就把方案定了。
要定的：壳（Electron / Tauri / 原生）、界面框架、本地存储（JSON 文件 / SQLite）、打包工具、自动更新方案、测试框架。每项写：选什么、版本、一句话为什么、一个备选。
最后写清本机要先装什么和验证命令，再用大白话说一遍。`,
      mobile: `读 docs/03-需求.md。我的情况：{{一句话}}

为这个手机 App 选一套技术栈，写到 docs/04-技术栈.md。选择原则：
- 成熟、文档全、你最熟的。不选小众的。
- 按我的水平选：跨平台（React Native 或 Flutter）一套代码两端跑，原生各写一遍。说清楚差别再推荐；用托管的构建服务省掉本机装 Xcode 的麻烦，如果可以。
- 上架要开发者账号和费用（Apple 每年、Google 一次性），现在就告诉我。
要定的：跨平台还是原生、导航库、状态管理、后端或 BaaS（认证、数据库、存储一起解决的优先）、推送、测试框架。每项写：选什么、版本、一句话为什么、一个备选。
最后写清本机要先装什么（Xcode / Android Studio / 模拟器）和验证命令，再用大白话说一遍。`,
      other: `读 docs/03-需求.md。我的情况：{{一句话}}

为这个项目选一套最省事的技术方案，写到 docs/04-技术栈.md。选择原则：
- 成熟、文档全、你最熟的，优先我已经会的。
- 越少越好，依赖能不加就不加。
要定的：语言与运行时、核心依赖、测试框架、运行方式（手动 / 定时 / 事件触发）、分发方式。每项写：选什么、版本、一句话为什么、一个备选。
最后写清本机要先装什么和验证命令，再用大白话说一遍。`
    }
  },
  {
    stage: 'plan',
    flow: 'fresh',
    name: '定架构与数据',
    hint: '模块怎么分、数据长什么样、代码要守什么规矩。这一步定对了，后面少返工。',
    output: 'docs/05-架构.md',
    byTrack: {
      web: `读 docs/03-需求.md 和 docs/04-技术栈.md。

定网站的架构，写到 docs/05-架构.md：
- 页面清单：每个页面一句话，谁能看（匿名 / 登录 / 只有本人）。
- 接口：按资源命名，写清每个接口的输入、输出、错误码；列表接口一开始就带分页。哪些代码跑在服务端、哪些跑在浏览器，边界画清楚。
- 数据模型：每张表的字段、类型、必填、关系、索引（按会查什么建）。定死这几个以后难改的：主键用什么、时间统一存 UTC、删除用软删除还是硬删、金额用整数分不用浮点。迁移只往前走，不改已发布的迁移。
- 认证与权限：哪些接口要登录、读写别人数据时怎么校验归属（每个按 id 取数据的接口都要）。
- 校验：所有外部输入（表单、接口参数、URL 参数）在服务端边界用 schema 校验一次，前端校验只是提示。
- 约定：错误怎么处理和返回、日志打什么不打什么（不打密码、token、身份证）、环境变量怎么读、命名规则。
- 第一版不做：缓存、队列、微服务、实时推送——除非需求里写了。
- 决定记录：每个有争议的选择写"选了什么、为什么、放弃了什么"。
写完列出最容易返工的 2–3 个决定，一次一个问我确认。`,
      cli: `读 docs/03-需求.md 和 docs/04-技术栈.md。

定命令行工具的架构，写到 docs/05-架构.md：
- 命令树：名词 + 动词的结构（如 tool item add），每个命令一句话、参数、选项。选项命名统一（长短两种、布尔用 --no- 取反）。
- 输入输出契约：数据走 stdout，提示和错误走 stderr；支持从 stdin 和管道读；--json 输出稳定格式给程序用；退出码约定（0 成功、1 用户错误、2 程序错误）。
- 配置优先级：命令行参数 > 环境变量 > 配置文件 > 默认值。配置文件放哪（按操作系统惯例）。
- 危险操作：会删改文件的命令要有 --dry-run 和 --yes；默认不覆盖已有文件。
- 分层：核心逻辑写成不依赖命令行的函数（好测），命令层只负责解析参数和打印。
- 路径：统一用绝对路径处理，不假设分隔符，不假设当前目录。
- 约定：错误信息格式（说清是什么、怎么改）、日志用 --verbose 才打、命名规则。
- 决定记录：每个有争议的选择写"选了什么、为什么、放弃了什么"。
写完列出最容易返工的 2–3 个决定，一次一个问我确认。`,
      desk: `读 docs/03-需求.md 和 docs/04-技术栈.md。

定桌面应用的架构，写到 docs/05-架构.md：
- 进程分工：主进程管文件、系统、窗口；渲染进程只管界面。所有文件和系统操作都在主进程做。
- 进程间通信：列出每个通道的名字、入参、返回值。渲染进程只能调白名单里的方法，拿不到 Node 和文件系统。
- 数据文件：放哪（按操作系统的用户数据目录）、什么格式、带版本号；写入先写临时文件再改名替换；启动时文件损坏怎么恢复（备份、隔离坏文件）。
- 窗口：单实例、记住位置和大小、关闭按钮的行为（退出还是最小化到托盘）。
- 设置：存哪、默认值、改了立即生效。
- 日志：写到文件、轮转、用户能找到；崩溃时保留现场。
- 自动更新：检查时机、下载后什么时候装、失败了怎么办——先定流程，代码后做。
- 约定：错误怎么处理和展示、命名规则、界面状态怎么管。
- 决定记录：每个有争议的选择写"选了什么、为什么、放弃了什么"。
写完列出最容易返工的 2–3 个决定，一次一个问我确认。`,
      mobile: `读 docs/03-需求.md 和 docs/04-技术栈.md。

定手机 App 的架构，写到 docs/05-架构.md：
- 页面和导航图：每个页面一句话、从哪进来、返回到哪；安卓实体返回键的行为。
- 状态分层：服务端数据缓存、本地持久数据、纯界面状态，各用什么管，别混在一起。
- 离线与同步：哪些操作要离线可用；本地先写再同步；两边都改了以谁为准（写死规则，比如最后写入赢）；同步失败了界面怎么表现。
- 接口契约：每个接口的输入、输出、错误；网络层统一超时、重试、错误映射成用户能看懂的提示。
- 认证：token 存 Keychain / Keystore，不存普通存储；过期怎么刷新。
- 权限清单：每个权限为什么要、什么时候申请、拒绝了怎么办。
- 平台差异：安全区、字体缩放、深色模式、键盘遮挡，一开始就用框架的标准做法。
- 约定：错误怎么处理和展示、日志不打个人信息、命名规则。
- 决定记录：每个有争议的选择写"选了什么、为什么、放弃了什么"。
写完列出最容易返工的 2–3 个决定，一次一个问我确认。`,
      other: `读 docs/03-需求.md 和 docs/04-技术栈.md。

定架构，写到 docs/05-架构.md：
- 模块划分：每个模块一句话职责，谁调用谁（mermaid 图）。能一个进程解决的不拆服务。
- 输入输出：从哪来、什么格式、校验什么；到哪去、失败了怎么办。
- 数据模型：每个实体的字段、类型、必填、关系。标出以后难改的（ID、时间时区、金额精度）。
- 运行方式：怎么触发、能不能重复跑（幂等）、跑一半挂了怎么恢复。
- 目录结构：到文件夹一级，每个文件夹一句话。
- 约定：错误怎么处理、日志怎么打、命名规则、配置怎么读。
- 决定记录：每个有争议的选择写"选了什么、为什么、放弃了什么"。
写完列出最容易返工的 2–3 个决定，一次一个问我确认。`
    }
  },

  // ---- 搭骨架 ----
  {
    stage: 'scaffold',
    flow: 'fresh',
    name: '初始化项目',
    hint: '创建空项目、装工具链、跑通 Hello World，顺便给 AI 立好规矩。你看着就行。',
    output: '跑起来的空项目 + CLAUDE.md',
    byTrack: {
      web: init('最后做一个能打开的首页，告诉我怎么在浏览器里看到它。我看到了才算完成。'),
      cli: init('最后做一个 --help 和 --version 能跑的最小命令，告诉我怎么在本机执行它。我跑通了才算完成。'),
      desk: init('最后做一个能打开的空窗口，告诉我怎么启动。我看到窗口了才算完成。'),
      mobile: init('最后做一个能在模拟器里打开的首屏，告诉我怎么在模拟器里看到它。我看到了才算完成。'),
      other: init('最后做一个能运行的最小入口，告诉我怎么运行它。我跑通了才算完成。')
    }
  },
  {
    stage: 'scaffold',
    flow: 'fresh',
    name: '基础设施',
    hint: '每种项目都有必须先铺好的底子：数据、登录、报错、配置。铺完才开始做功能。',
    output: '基础能力就绪 + 冒烟测试',
    byTrack: {
      web: `读 docs/05-架构.md 和 CLAUDE.md。

铺好网站的底子，做一项验证一项：
- 数据库：连接池、迁移工具、第一个迁移（按数据模型建表和索引）、本地一条命令重置并灌测试数据。
- 认证：注册 / 登录 / 登出 / 会话，用成熟库或托管服务，不自己写密码哈希和会话；登录接口加限流；cookie 设 httpOnly + secure + sameSite。
- 环境变量：启动时集中读取并用 schema 校验，缺了直接报错说缺哪个；区分服务端变量和能暴露给浏览器的变量。
- 输入校验：一个统一的校验层，所有接口的入参都过它。
- 错误处理：统一的错误类型和返回格式，用户看到的是人话，日志里有细节和请求 id；未捕获的异常不能让进程静默死掉；生产环境不返回堆栈。
- 健康检查接口，和一个最小的端到端冒烟测试（能注册、能登录、未登录访问受保护页面被拦）。
每项做完给我一个我能自己动手验证的方法。全部跑通后提交。`,
      cli: `读 docs/05-架构.md 和 CLAUDE.md。

铺好命令行工具的底子，做一项验证一项：
- 参数与子命令结构，--help 自动生成，--version 读版本号。
- 配置文件：默认值、校验、按操作系统放到约定位置，命令行参数和环境变量能覆盖。
- 输出：人看的和 --json 两种；stdout 只放数据，stderr 放提示；检测到不是终端就关掉颜色和进度条；退出码按约定。
- 错误处理：用户错误给人话和怎么改，程序错误给上下文；默认不打堆栈，--verbose 才打；Ctrl+C 能干净退出。
- 危险操作的 --dry-run 和 --yes 骨架。
- 一个冒烟测试：以子进程方式跑主命令，检查退出码和输出。
每项做完给我一个我能自己验证的方法。全部跑通后提交。`,
      desk: `读 docs/05-架构.md 和 CLAUDE.md。

铺好桌面应用的底子，做一项验证一项：
- 窗口与托盘：单实例锁、记住窗口位置和大小、关闭按钮的行为按架构文档。
- 进程间通信：contextIsolation 开、nodeIntegration 关、sandbox 按框架推荐；preload 只暴露白名单方法；主进程对每个入参做校验。
- 本地数据存储：用户数据目录、原子写入（临时文件 + 改名）、启动时自动备份、文件带版本号和升级函数、损坏文件隔离并提示。
- 设置持久化，主题跟随系统。
- 错误处理：渲染进程崩溃有提示能重开；写文件失败要弹窗告诉用户，不能静默丢数据；日志写到文件。
- 外部链接只允许 http(s) 用系统浏览器打开。
- 一个冒烟测试：启动、写一条数据、重启还在。
每项做完给我一个我能自己验证的方法。全部跑通后提交。`,
      mobile: `读 docs/05-架构.md 和 CLAUDE.md。

铺好手机 App 的底子，做一项验证一项：
- 导航结构和基础页面骨架，安全区和键盘遮挡处理好。
- 权限申请：先用自己的页面解释为什么要，再弹系统框；拒绝了要有备用路径，不反复弹。
- 本地存储与离线：数据先存本地再同步，断网能看能改；token 存 Keychain / Keystore。
- 网络层：超时、重试、统一错误映射；每个页面都有加载中、空、失败三种状态。
- 环境配置：开发 / 生产两套接口地址，密钥不进代码和打包产物。
- 崩溃上报接进去。
- 一个冒烟测试：能启动、能进主要页面；模拟器上真跑一遍。
每项做完给我一个我能自己验证的方法。全部跑通后提交。`,
      other: `读 docs/05-架构.md 和 CLAUDE.md。

铺好底子，做一项验证一项：
- 配置读取：默认值、校验、缺了直接报错说缺哪个；密钥从环境变量读。
- 日志：级别可调，出错时有足够上下文，不打密钥和个人信息。
- 错误处理：统一的错误类型，用户看到的是人话，不吞异常；跑一半失败能安全重跑。
- 测试脚手架和一个冒烟测试。
每项做完给我一个我能自己验证的方法。全部跑通后提交。`
    }
  },

  // ---- 做功能（循环） ----
  {
    stage: 'build',
    flow: 'existing',
    name: '读懂现有代码',
    hint: '接手的是已有的代码。先让 AI 看一遍、跑一遍、立好规矩，再动手。',
    output: 'docs/00-现状.md + CLAUDE.md',
    prompt: `先看一遍这个仓库：README、依赖文件和锁文件、入口、目录结构、有没有测试和 CI、最近 20 条提交在改什么。先别改任何东西。

写 docs/00-现状.md（没有 docs 文件夹就先建）：
- 5 句话：这是什么、给谁用、怎么跑起来、怎么测、现在能不能正常跑（你实际跑一下，把结果写上）。
- 主要模块和依赖关系（mermaid 图）；改一个功能通常要动哪几个文件。
- 现有约定：命名、目录、错误处理、状态管理、数据怎么存。后面写代码要跟着走，哪怕你觉得不好。
- 风险和技术债：按"改功能时会踩到"的程度排序，只列前 5；过期或有漏洞的依赖单独列。
- 看不懂的地方列出来，一次一个问我。
如果没有 CLAUDE.md，写一个：怎么跑、怎么测、代码约定、改动前先读相关文件、改完必须跑检查和测试、密钥不进代码、不确定就问我。有的话补上缺的。`
  },
  {
    stage: 'build',
    name: '描述功能 → 拆任务',
    question: '这次做哪个功能？',
    hint: '一次只做一个功能。你用一句话说，AI 先写清楚做完什么样，再拆成小任务。',
    output: 'docs/tasks/<功能>.md',
    prompt: `读 docs/03-需求.md（有的话）、docs/05-架构.md 或 docs/00-现状.md、CLAUDE.md。这次要做的功能：{{一句话}}

先不要写代码。写 docs/tasks/<功能名>.md：
- 验收标准：做完后用户能做到什么，写成可以逐条打勾的具体行为，包括出错和边界情况（空、重复、超长、没权限、没网、并发点两次）。
- 要动的文件和原因；要不要改数据模型，改了老数据怎么迁移；要不要新加依赖，为什么现有的不够。
- 拆成 3–8 个小任务，每个 30 分钟内能做完、能独立验证、写出验证方法。顺序：先数据和核心逻辑，再界面，最后收尾。用 - [ ] 列出。
- 风险和不确定的点，一次一个问我。
我确认任务清单后再开始。`
  },
  {
    stage: 'build',
    name: '实现',
    hint: '按清单一个一个做，每做完一个自己检查、提交、停下。在 Claude Code 里说「继续」做下一个，全部勾完再点下一步。',
    output: '代码 + 勾选的任务',
    prompt: `读 CLAUDE.md 和 docs/tasks/ 里当前功能的任务文件。

做第一个还没勾的任务，只做这一个：
- 先读会碰到的文件，跟现有风格和约定走；改动最小，不顺手重构别的，不加任务清单外的东西。
- 不自己造轮子：项目里已有的工具、框架自带的能力优先；要加依赖先问我。
- 外部输入要校验，错误要处理，不吞异常，不写 TODO 糊过去。
- 做完跑 typecheck、lint、test，全过才算做完；失败了修，不要跳过、不要注释掉测试。
- 勾掉任务，git commit，一句话说明做了什么。
告诉我：改了哪些文件、怎么验证、有没有留下待办。然后停下等我说「继续」。`
  },
  {
    stage: 'build',
    name: '测试',
    hint: '功能做完了，让 AI 按验收标准补测试。以后改坏了能立刻知道。',
    output: '测试通过',
    byTrack: {
      web: `读 docs/tasks/ 里当前功能的任务文件、它的验收标准，和这次改的代码。

为这个功能补测试，分三层，别都堆在一层：
- 单元：纯逻辑（计算、校验、格式化），不碰数据库和网络。
- 接口：用真实的测试数据库跑（每个用例前重置），覆盖：正常、入参非法返回 4xx 且有错误信息、未登录 401、访问别人的数据 403、不存在 404、重复提交。
- 端到端：只挑这个功能最核心的 1 条用户路径，用浏览器自动化跑一遍。
- 测行为不测实现：测试名要能读出"什么情况下期望什么"；不 mock 自己的代码，只 mock 第三方服务、时间、随机。
跑全部测试，把结果贴给我。有失败的先判断是测试错还是代码错，再修。哪些行为故意不测，一句话说明理由。测试通过后提交。`,
      cli: `读 docs/tasks/ 里当前功能的任务文件、它的验收标准，和这次改的代码。

为这个功能补测试：
- 核心逻辑：直接调函数测，不经过命令行。
- 命令层：以子进程方式跑真实命令，检查退出码、stdout、stderr 分开断言；--json 输出用快照测，--help 输出也用快照。
- 覆盖：参数缺失或非法时的错误信息、配置优先级（参数 > 环境变量 > 配置文件）、从 stdin 读、--dry-run 不改文件、大输入不爆内存。
- 文件操作全在临时目录里做，测完清掉；路径断言不写死分隔符。
- 测行为不测实现：测试名要能读出"什么情况下期望什么"。
跑全部测试，把结果贴给我。有失败的先判断是测试错还是代码错，再修。哪些行为故意不测，一句话说明理由。测试通过后提交。`,
      desk: `读 docs/tasks/ 里当前功能的任务文件、它的验收标准，和这次改的代码。

为这个功能补测试：
- 主进程逻辑：存储、迁移、业务函数直接测，用临时目录当用户数据目录。
- 存储专门测：写入中断不丢旧数据、损坏文件能恢复、老版本数据文件能升级、并发写不打架。
- 进程间通信：每个处理函数用非法入参调一次，必须拒绝而不是崩。
- 界面：只测有逻辑的组件（状态切换、表单校验），纯展示的不测。
- 最多一个真启动的冒烟测试：开应用、做一个操作、重启数据还在。
- 测行为不测实现：测试名要能读出"什么情况下期望什么"；只 mock 系统对话框、网络、时间。
跑全部测试，把结果贴给我。有失败的先判断是测试错还是代码错，再修。哪些行为故意不测，一句话说明理由。测试通过后提交。`,
      mobile: `读 docs/tasks/ 里当前功能的任务文件、它的验收标准，和这次改的代码。

为这个功能补测试：
- 逻辑层：状态变化、数据转换、同步规则直接测，不渲染界面。
- 离线：断网时操作被本地记下、恢复后同步、两边冲突按定好的规则处理。
- 组件：有交互和状态的页面测（输入校验、加载 / 空 / 失败三种状态都渲染对），纯展示的不测。
- 权限拒绝路径至少一条。
- 模拟器上真跑一遍这个功能，两个平台都要（如果都支持），把看到的问题记下来。
- 测行为不测实现：测试名要能读出"什么情况下期望什么"；只 mock 网络、时间、系统权限。
跑全部测试，把结果贴给我。有失败的先判断是测试错还是代码错，再修。哪些行为故意不测，一句话说明理由。测试通过后提交。`,
      other: `读 docs/tasks/ 里当前功能的任务文件、它的验收标准，和这次改的代码。

为这个功能补测试：
- 按验收标准逐条写，每条至少一个测试；再加边界（空、重复、超长、并发）和出错路径。
- 输入输出用真实样例文件测；跑一半失败后重跑不会产生重复结果。
- 测行为不测实现：测试名要能读出"什么情况下期望什么"；只 mock 网络、时间、随机。
跑全部测试，把结果贴给我。有失败的先判断是测试错还是代码错，再修。哪些行为故意不测，一句话说明理由。测试通过后提交。`
    }
  },
  {
    stage: 'build',
    name: '审一遍',
    hint: '代码质量 + 安全一起看。严重的 AI 直接修，其余问你。修完这个功能就算完成。',
    output: '问题清单 + 修复',
    byTrack: {
      web: review(
        'SQL 是否全部参数化；渲染用户内容有没有绕过转义；每个按 id 取数据的接口有没有校验归属（越权）；改数据的接口有没有 CSRF 保护和登录校验；密钥有没有进浏览器包或前端环境变量；登录、注册、发邮件有没有限流；上传有没有限制类型和大小；错误响应有没有泄露堆栈或内部信息；循环里查库（N+1）和缺索引的查询'
      ),
      cli: review(
        '调外部命令是不是用参数数组而不是拼字符串；用户给的路径有没有做规范化防止跳出目录；会覆盖或删除文件的地方有没有确认或 --dry-run；密钥和 token 有没有打进日志或明文写进配置；网络请求有没有超时；大文件是不是流式处理；Windows 路径和换行有没有被写死；临时文件有没有清理'
      ),
      desk: review(
        'nodeIntegration 是否关、contextIsolation 是否开；每个 IPC 处理函数有没有校验入参；渲染进程能不能拿到任意路径的读写；外部链接和文件打开是不是只允许安全协议；密钥用没用系统级加密存储而不是明文；写文件是不是原子的；自动更新有没有校验签名；事件监听有没有在窗口关闭时移除（泄漏）；主进程有没有被同步 IO 卡住界面'
      ),
      mobile: review(
        '密钥和接口 key 有没有打进安装包；token 是不是在 Keychain / Keystore；日志和崩溃上报里有没有个人信息；申请的权限是不是最小必要；深链接和外部传入的参数有没有校验；离线时的写操作恢复后会不会重复提交；网络请求有没有超时和取消（页面退出后回调还在改状态）；长列表有没有虚拟化；主线程有没有做重活；证书是否校验（没关掉 SSL 校验）'
      ),
      other: review('外部输入有没有校验；密钥是不是从环境变量读且没进日志；失败重跑会不会产生重复副作用；外部调用有没有超时和重试上限；临时文件和资源有没有释放')
    }
  },

  // ---- 上线 ----
  {
    stage: 'ship',
    name: '发布',
    hint: '让别人用上。AI 写成逐步清单，你做一步它确认一步。',
    output: 'docs/06-上线.md + 线上 / 可安装的版本',
    byTrack: {
      web: `读 docs/04-技术栈.md 和 .env.example。

把网站部署上线。先写逐步清单到 docs/06-上线.md，然后我做一步你确认一步：
- 上线前：密钥不在代码里、生产环境变量清单（和 .env.example 对得上）、生产用独立的数据库、迁移在生产怎么跑、数据库自动备份开了没。
- 部署平台配置、自定义域名和 HTTPS、CI：推 main 自动跑测试再部署，测试不过不部署。
- 上线后：健康检查地址、怎么看日志和错误、怎么回滚到上一版（先演练一次）。
- 基础防护：登录限流在生产生效、错误页不露内部信息。
需要我在网页上点的步骤写清楚点哪里。每一步给我验证方法。`,
      cli: `读 docs/04-技术栈.md。

发布这个命令行工具。先写逐步清单到 docs/06-上线.md，然后我做一步你确认一步：
- 版本号（语义化）、变更日志、README 里的安装和使用说明、许可证文件。
- 发布到 npm（或构建各平台二进制并附到 GitHub Release），发布由 CI 打 tag 触发，不在本机手动发。
- 发布前先打包到本地装一遍看包里有没有多余文件或缺文件。
- 发布后在一台干净环境装一遍验证：一条命令能装上、--version 对、--help 能看。
每一步给我验证方法，告诉我以后发新版怎么做。`,
      desk: `读 docs/04-技术栈.md。

发布这个桌面应用。先写逐步清单到 docs/06-上线.md，然后我做一步你确认一步：
- 版本号、变更日志、应用图标各尺寸。
- 三平台打包，在 CI 里跑，产物附到 GitHub Release。
- 代码签名与公证：怎么做、要多少钱、没签名时 Windows SmartScreen 和 macOS Gatekeeper 各会提示什么、用户怎么绕过——写进 README。
- 自动更新通道：从上一版更新到这一版实际试一次。
- 发布后在一台干净机器上装一遍验证，卸载也试一次。
每一步给我验证方法，告诉我以后发新版怎么做。`,
      mobile: `读 docs/04-技术栈.md。

发布这个手机 App。先写逐步清单到 docs/06-上线.md，然后我做一步你确认一步：
- 开发者账号和费用、应用标识和签名证书（备份好，丢了发不了新版）、图标与截图规格、隐私政策页面和权限用途说明。
- 版本号和构建号的规则。
- 崩溃上报确认在生产包里生效。
- TestFlight / 内测轨道先发给几个人，真机上试过再提交审核。
- 正式上架审核要点和常见被拒原因（缺隐私说明、登录没有演示账号、权限用途没写、有占位内容），逐条对照。
每一步给我验证方法，告诉我以后发新版怎么做。`,
      other: `读 docs/04-技术栈.md。

发布这个项目。先写逐步清单到 docs/06-上线.md，然后我做一步你确认一步：
- 版本号、变更日志、安装与使用说明、许可证。
- 打包或发布到对应的包管理器，由 CI 打 tag 触发。
- 发布后在干净环境装一遍验证。
- 定时或事件触发的任务：确认在目标环境里真跑了一次，失败时有人能收到通知。
每一步给我验证方法，告诉我以后发新版怎么做。`
    }
  }
]
